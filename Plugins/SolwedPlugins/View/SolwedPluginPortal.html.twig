{#
/**
 * SolwedPlugins - Plugin Manager
 * @deprecated Esta página ha sido integrada en Admin > Plugins > Portal Solwed
 *
 * Si ves este mensaje, significa que la redirección no funcionó.
 * Por favor, accede a: Admin > Plugins > Portal Solwed
 */
#}
{% extends "Master/MenuBghTemplate.html.twig" %}

{% block body %}
    {{ parent() }}
    <div class="container mt-5">
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <h4 class="alert-heading">
                <i class="fas fa-info-circle mr-2"></i> Página Deprecada
            </h4>
            <p>
                El Portal Solwed ha sido integrado completamente en
                <strong>Admin > Plugins > Portal Solwed</strong>
            </p>
            <hr>
            <p class="mb-0">
                <a href="AdminPlugins" class="btn btn-primary">
                    <i class="fas fa-arrow-right mr-1"></i> Ir a Admin > Plugins
                </a>
            </p>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    </div>
{% endblock %}
