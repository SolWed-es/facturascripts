{#
* Copyright (C) 2022 Carlos Garcia Gomez <carlos@facturascripts.com>
* Author Daniel Fernández Giménez <hola@danielfg.es>
#}

{% extends "Master/MenuBghTemplate.html.twig" %}

{% block body %}
    {{ parent() }}
    <div class="bg-light pt-3 pb-5">
        <div class="container">
            <div class="row d-flex align-items-center">
                <div class="col">
                    <h1 class="h2">
                        <i class="fas fa-cash-register"></i> {{ trans('select-tpv') }}
                    </h1>
                </div>
                <div class="col text-right">
                    <div class="btn-group">
                        <a class="btn btn-sm btn-secondary" href="{{ fsc.url() }}"
                           title="{{ trans('refresh') }}">
                            <i class="fas fa-redo" aria-hidden="true"></i>
                        </a>
                        {% if pageData.name == fsc.user.homepage %}
                            <a class="btn btn-sm btn-secondary active" href="{{ fsc.url() }}?defaultPage=FALSE"
                               title="{{ trans('marked-as-homepage') }}">
                                <i class="fas fa-bookmark" aria-hidden="true"></i>
                            </a>
                        {% else %}
                            <a class="btn btn-sm btn-secondary" href="{{ fsc.url() }}?defaultPage=TRUE"
                               title="{{ trans('mark-as-homepage') }}">
                                <i class="far fa-bookmark" aria-hidden="true"></i>
                            </a>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container" style="margin-top: -40px;">
        <div class="row">
            <div class="col-sm">
                {% if fsc.tpvs is empty %}
                    <div class="card shadow mb-4">
                        <div class="card-body text-center">
                            <div class="w-100 pb-4">
                                <img src="{{ asset('Dinamic/Assets/Images/tpvneo-wizard.png') }}" class="img-fluid"
                                     alt="tpv"/>
                            </div>
                            <div class="h3">{{ trans('no-tpv-terminal') }}</div>
                            <p class="card-text">{{ trans('no-tpv-terminal-legend') }}</p>
                        </div>
                        <div class="card-footer text-center">
                            {% if fsc.user.admin %}
                                <a href="{{ asset('EditTpvTerminal') }}?action=fast-config"
                                   class="btn btn-success btn-lg mr-2">
                                    {{ trans('fast-tpv-config') }}
                                </a>
                                <a href="{{ asset('EditTpvTerminal') }}" class="btn btn-outline-success btn-lg">
                                    {{ trans('add-tpv') }}
                                </a>
                            {% else %}
                                <a href="{{ asset('EditTpvTerminal') }}" class="btn btn-success btn-lg">
                                    {{ trans('add-tpv') }}
                                </a>
                            {% endif %}
                        </div>
                    </div>
                {% else %}
                    <div class="card shadow mb-4">
                        <div class="card-body">
                            <div class="w-100 text-center pb-4">
                                <img src="{{ asset('Dinamic/Assets/Images/tpvneo-wizard.png') }}" class="img-fluid"
                                     alt="tpv"/>
                            </div>
                        </div>
                        <div class="card-footer">
                            <div class="form-row">
                                {% for tpv in fsc.tpvs %}
                                    <div class="col-sm">
                                        <form action="{{ fsc.url() }}" method="post">
                                            <input type="hidden" name="idtpv" value="{{ tpv.idtpv }}">
                                            <button type="submit" name="action" value="select-tpv"
                                                    class="btn btn-lg btn-block btn-primary">
                                                <i class="fas fa-cash-register mr-1"></i> {{ tpv.name }}
                                            </button>
                                        </form>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
    <iframe class="d-none" src="http://localhost:8089"></iframe>
{% endblock %}