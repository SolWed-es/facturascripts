{#
 * SolwedShadcn — Utils: macros de utilidad con estilo shadcn
 * message, messageCompat, popoverTitle, registerInstall, updateInstall
 #}

{# Muestra todos los mensajes con alguno de los niveles dados #}
{% macro message(log, levels, style) %}
    {% set messages = log.read('master', levels) | merge(log.read('database', levels)) %}
    {% if messages | length > 0 %}
        <div class="alert alert-dismissible alert-{{ style }}" role="alert">
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            {% for item in messages %}
                <div>{{ item.message }}</div>
            {% endfor %}
        </div>
    {% endif %}
{% endmacro %}

{# Muestra todos los mensajes (compat versiones antiguas MiniLog) #}
{% macro messageCompat(log, levels, style) %}
    {% for item in log.read() %}
        {% if item.level in levels and item.channel == 'master' %}
            <div class="alert alert-dismissible alert-{{ style }}" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                <div>{{ item.message }}</div>
            </div>
        {% endif %}
    {% endfor %}
{% endmacro %}

{# Parámetros para popover Bootstrap #}
{% macro popoverTitle(msg, position = 'auto') %}
    {% if msg | length > 0 %}data-bs-toggle="popover" data-placement="{{ position }}" data-trigger="hover" data-content="{{ msg }}"{% endif %}
{% endmacro %}

{# Mensaje para registrar la instalación #}
{% macro registerInstall(link = true) %}
    <div class="card-shadcn">
        <div class="p-4">
            <div class="flex items-start gap-3">
                <div class="inline-flex items-center justify-center w-10 h-10 rounded-md bg-info/10 flex-shrink-0">
                    <i data-lucide="badge-check" class="w-5 h-5 text-info"></i>
                </div>
                <div>
                    <h3 class="font-semibold mb-1">Conecta tu instalación con SolWed Mind</h3>
                    <p class="text-sm text-muted-foreground">Conecta tu ERP con SolWed Mind para recibir actualizaciones, soporte y acceso al Portal de plugins.</p>
                </div>
            </div>
        </div>
        {% if link %}
            <div class="border-t border-border p-2 flex items-center gap-2">
                <a href="{{ asset('Updater') }}" class="btn btn-sm btn-secondary">
                    <i data-lucide="shield-check" class="w-3.5 h-3.5 me-1"></i> {{ trans('register') }}
                </a>
                <a href="https://solwed.es/patrocinar" target="_blank" class="btn btn-sm btn-outline-secondary">
                    {{ trans('become-sponsor') }}
                </a>
            </div>
        {% else %}
            <div class="border-t border-border p-2">
                <button class="btn btn-sm btn-secondary" type="button" data-bs-toggle="modal" data-bs-target="#modalTelemetry">
                    <i data-lucide="shield-check" class="w-3.5 h-3.5 me-1"></i> {{ trans('register') }}
                </button>
            </div>
        {% endif %}
    </div>
{% endmacro %}

{# Mensaje para actualizar la instalación #}
{% macro updateInstall() %}
    <div class="card-shadcn border-l-4 border-l-warning">
        <div class="p-4">
            <div class="flex items-start gap-3">
                <div class="inline-flex items-center justify-center w-10 h-10 rounded-md bg-warning/10 flex-shrink-0">
                    <i data-lucide="cloud-download" class="w-5 h-5 text-warning"></i>
                </div>
                <div>
                    <h3 class="font-semibold mb-1">{{ trans('updates-available') }}</h3>
                    <p class="text-sm text-muted-foreground">{{ trans('updates-available-p') }}</p>
                </div>
            </div>
        </div>
        <div class="border-t border-border p-2">
            <a href="{{ asset('Updater') }}" class="btn btn-sm btn-warning">
                <i data-lucide="download" class="w-3.5 h-3.5 me-1"></i> {{ trans('update') }}
            </a>
        </div>
    </div>
{% endmacro %}
