{# SolwedShadcn — Archivos de documento #}
{% set firstView = fsc.views | first %}
{% set doc = firstView.model %}
{% set currentView = fsc.getCurrentView() %}

<div class="container-fluid">
    <div class="row">
        <div class="col-sm-12">
            {% if fsc.permissions.allowUpdate %}
                <form action="{{ doc.url() }}" method="post" enctype="multipart/form-data"
                      onsubmit="animateSpinner('add')">
                    {{ formToken() }}
                    <input type="hidden" name="activetab" value="{{ currentView.getViewName() }}"/>
                    <input type="hidden" name="action" value="add-file"/>
                    <div class="card-shadcn border-success mb-3">
                        <div class="p-3">
                            <h5 class="font-semibold mb-3 flex items-center gap-2">
                                <i data-lucide="folder-plus" class="w-5 h-5"></i> {{ trans('add') }}
                            </h5>
                            <div class="mb-3">
                                <textarea name="observations" class="form-control"
                                          placeholder="{{ trans('observations') }}"></textarea>
                            </div>
                            {% for includeView in getIncludeViews('DocFiles', 'formAddBody') %}
                                {% include includeView['path'] %}
                            {% endfor %}
                            <div class="row align-items-end">
                                <div class="col">
                                    <p class="text-sm text-muted-foreground mb-2">
                                        {{ trans('help-server-accepts-filesize', {'%size%': currentView.model.getMaxFileUpload()}) }}
                                    </p>
                                    <input type="file" name="new-files[]" class="form-control" multiple required/>
                                </div>
                                <div class="col-auto">
                                    <button type="submit" class="btn btn-success">
                                        <i data-lucide="save" class="w-4 h-4 me-1"></i> {{ trans('save') }}
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            {% endif %}

            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-3">
                {% for docfile in currentView.cursor %}
                    {% set file = docfile.getFile() %}
                    {% if file %}
                    <div class="col">
                        <form action="{{ doc.url() }}" method="post" enctype="multipart/form-data"
                              onsubmit="animateSpinner('add')">
                            {{ formToken() }}
                            <input type="hidden" name="activetab" value="{{ currentView.getViewName() }}"/>
                            <input type="hidden" name="id" value="{{ docfile.id }}"/>
                            <input type="hidden" name="action" value=""/>
                            <div class="card-shadcn h-full">
                                {% if file.isImage() %}
                                    <a href="{{ file.url('download-permanent') }}" target="_blank">
                                        <img src="{{ file.url('download') }}" class="w-full rounded-t-lg"
                                             alt="{{ file.filename }}" loading="lazy">
                                    </a>
                                {% else %}
                                    <div class="p-3">
                                        <a href="{{ file.url('download-permanent') }}" target="_blank"
                                           class="btn w-100 btn-lg btn-secondary" aria-label="{{ trans('download') }} {{ file.filename }}">
                                            {% if file.isPdf() %}
                                                <i data-lucide="file-text" class="w-5 h-5 me-1"></i>
                                            {% elseif file.isVideo() %}
                                                <i data-lucide="video" class="w-5 h-5 me-1"></i>
                                            {% elseif file.isArchive() %}
                                                <i data-lucide="archive" class="w-5 h-5 me-1"></i>
                                            {% else %}
                                                <i data-lucide="file" class="w-5 h-5 me-1"></i>
                                            {% endif %}
                                            <span class="text-break">{{ file.filename | raw }}</span>
                                        </a>
                                    </div>
                                {% endif %}
                                <div class="p-3">
                                    <div class="mb-3">
                                        <label for="observations-{{ docfile.id }}" class="visually-hidden">{{ trans('observations') }}</label>
                                        <textarea name="observations" id="observations-{{ docfile.id }}" class="form-control"
                                                  placeholder="{{ trans('observations') }}">{{ docfile.observations | raw }}</textarea>
                                    </div>
                                    {% for includeView in getIncludeViews('DocFiles', 'formEditBody') %}
                                        {% include includeView['path'] %}
                                    {% endfor %}
                                    <p class="text-muted-foreground text-xs flex items-center flex-wrap gap-x-3">
                                        {% if docfile.nick %}
                                            <span class="inline-flex items-center gap-1">
                                                <i data-lucide="user" class="w-3 h-3"></i> {{ docfile.nick }}
                                            </span>
                                        {% endif %}
                                        <span class="inline-flex items-center gap-1">
                                            <i data-lucide="calendar" class="w-3 h-3"></i> {{ docfile.creationdate }}
                                        </span>
                                        <span class="inline-flex items-center gap-1">
                                            <i data-lucide="hard-drive" class="w-3 h-3"></i> {{ bytes(file.size) }}
                                        </span>
                                    </p>
                                </div>
                                <div class="border-t border-border p-2">
                                    <div class="flex items-center justify-between">
                                        {% if fsc.permissions.allowDelete %}
                                            <div class="dropdown">
                                                <button class="btn btn-sm btn-outline-danger dropdown-toggle" type="button"
                                                        data-bs-toggle="dropdown" aria-expanded="false"
                                                        aria-label="{{ trans('delete-options') }}">
                                                    <i data-lucide="trash-2" class="w-3.5 h-3.5"></i>
                                                </button>
                                                <ul class="dropdown-menu">
                                                    <li>
                                                        <button type="button"
                                                                onclick="this.form.action.value='unlink-file';this.form.onsubmit();this.form.submit();"
                                                                class="dropdown-item">
                                                            <i data-lucide="unlink" class="w-4 h-4 me-1"></i> {{ trans('unlink-file') }}
                                                        </button>
                                                    </li>
                                                    <li><hr class="dropdown-divider"></li>
                                                    <li>
                                                        <button type="button"
                                                                onclick="if(confirm('{{ trans('confirm-delete') }}')) { this.form.action.value='delete-file';this.form.submit(); }"
                                                                class="dropdown-item text-danger">
                                                            <i data-lucide="trash-2" class="w-4 h-4 me-1"></i> {{ trans('delete-file') }}
                                                        </button>
                                                    </li>
                                                </ul>
                                            </div>
                                        {% endif %}
                                        {% if fsc.permissions.allowUpdate %}
                                            <button type="button" class="btn btn-sm btn-primary"
                                                    onclick="this.form.action.value='edit-file';this.form.onsubmit();this.form.submit();">
                                                <i data-lucide="save" class="w-3.5 h-3.5 me-1"></i> {{ trans('save') }}
                                            </button>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                        </form>
                    </div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
    </div>
</div>
