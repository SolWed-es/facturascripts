{# SolwedShadcn — Controller no encontrado #}
{% extends "Master/MicroTemplate.html.twig" %}

{% block body %}
    {{ parent() }}
    <div class="flex items-center justify-center min-h-[60vh] px-4">
        <div class="card-shadcn max-w-lg w-full">
            <div class="p-8 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-warning/10 mb-4">
                    <i data-lucide="bug" class="w-8 h-8 text-warning"></i>
                </div>
                <h1 class="text-xl font-heading font-semibold mb-2">{{ trans('controller-not-found') }}</h1>
                <p class="text-muted-foreground text-sm mb-1 font-medium">{{ controllerName }}</p>
                <div class="mt-6">
                    <a href="{{ asset('Wizard') }}" class="btn btn-warning btn-sm">
                        <i data-lucide="wrench" class="w-4 h-4 me-1"></i> {{ trans('wizard') }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
