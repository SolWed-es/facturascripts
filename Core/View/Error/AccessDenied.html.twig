{# SolwedShadcn — Acceso denegado #}
{% extends "Master/MenuBghTemplate.html.twig" %}

{% block bodyHeaderOptions %}
{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-[60vh] px-4">
        <div class="card-shadcn max-w-lg w-full">
            <div class="p-8 text-center">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-destructive/10 mb-4">
                    <i data-lucide="lock" class="w-8 h-8 text-destructive"></i>
                </div>
                <h1 class="text-xl font-heading font-semibold mb-2">{{ trans('access-denied') }}</h1>
                <p class="text-muted-foreground text-sm mb-1">{{ controllerName }}</p>
                <p class="text-muted-foreground text-sm">{{ trans('access-denied-p') }}</p>
                <div class="mt-6">
                    <a href="Dashboard" class="btn btn-primary btn-sm">
                        <i data-lucide="home" class="w-4 h-4 me-1"></i> {{ trans('dashboard') }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
