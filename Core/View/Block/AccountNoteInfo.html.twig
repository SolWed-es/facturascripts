{# SolwedShadcn — Info conceptos generales contabilidad #}
<h4 class="text-info font-heading font-semibold">{{ trans('general-concepts') }}</h4>
<div class="row">
    <div class="col-sm-3 fw-bold">{{ trans('wildcard') }}</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('wildcard-general-info') }}</div>
</div>
<h4 class="mt-4 text-info font-heading font-semibold">{{ trans('wildcard-title') }}</h4>
<div class="row">
    <div class="col-sm-3 fw-bold">%document%</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('document-wildcard-info') }}</div>
</div>
<div class="row">
    <div class="col-sm-3 fw-bold">%date%</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('date-wildcard-info') }}</div>
</div>
<div class="row">
    <div class="col-sm-3 fw-bold">%date-entry%</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('dateentry-wildcard-info') }}</div>
</div>
<div class="row">
    <div class="col-sm-3 fw-bold">%month%</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('month-wildcard-info') }}</div>
</div>
<div class="row">
    <div class="col-sm-3 fw-bold">%year%</div>
    <div class="col-sm-9 text-muted-foreground">{{ trans('year-wildcard-info') }}</div>
</div>
